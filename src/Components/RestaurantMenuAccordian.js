const RestaurantMenuAccordion = ({
    cardDetails,
    isAccordionOpen,
    setShowIndex,
    resDetails
  }) => {
    const details = cardDetails;
    return(
        <div>
            Items
        </div>
    )
}

export default RestaurantMenuAccordion